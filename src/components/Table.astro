<table role="table">
  <thead>
    <tr role="row">
      <slot name="header" />
    </tr>
  </thead>
  <tbody>
    <slot name="body" />
  </tbody>
</table>

<style>
  /* Die Tabelle nimmt immer 100% der Breite des Containers ein */
  table {
    width: 100%;
    border-collapse: collapse;
    table-layout: fixed; /* Stellt sicher, dass die Spalten gleichmäßig verteilt werden */
  }

  /* Die Tabellenzellen */
  th,
  td {
    border: 1px solid #ccc;
    padding: 12px;
    text-align: left;
    font-size: 1rem;
    width: 100%; /* Die Zellen nehmen die volle Breite ein */
  }

  th {
    background-color: #f4f4f4;
  }

  /* Responsive Anpassungen für kleine Bildschirme */
  @media (max-width: 768px) {
    table,
    thead,
    tbody,
    th,
    td,
    tr {
      display: block;
    }

    thead {
      display: none; /* Spaltenüberschriften in der mobilen Ansicht ausblenden */
    }

    tr {
      margin-bottom: 15px;
    }

    td {
      display: flex;
      justify-content: space-between;
      padding: 12px;
      border-bottom: 1px solid #eee;
      position: relative;
      width: 100%; /* Zellen nutzen die volle Breite */
    }

    td::before {
      content: attr(data-label);
      position: absolute;
      left: 16px;
      top: 12px;
      font-weight: bold;
      font-size: 0.9rem;
      text-transform: uppercase;
      color: #555;
    }

    td {
      padding-left: 50%; /* Abstand zwischen der Beschriftung und dem Inhalt */
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }
  }
</style>
